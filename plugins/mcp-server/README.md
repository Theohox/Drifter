# MCP Server Plugin Stub

**Status:** Future / Backlog (PLUG-002)

This directory is reserved for an MCP (Model Context Protocol) server plugin.

Planned functionality:
- Expose Drifter checks as MCP tools
- Allow agents to query drift state via MCP
- Integrate with MCP-compatible IDEs

See `docs/project-conductor.md` for scheduling.
